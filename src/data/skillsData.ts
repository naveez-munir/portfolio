import { SkillTabs } from "@/types/skills";
import { Code, Database, Server } from "lucide-react";

export const skillTabData: SkillTabs[] = [
  { name: "Frontend", icon: Code, },
  { name: "Backend", icon: Server, },
  { name: "Full Stack", icon: Database, },
]
